const profiles = (state = {}, action) => {
    switch (action.type) {
        case 'REQUEST_PROFILES':
            return state
            break;
        default:
            return state
    }
}

export default profiles
